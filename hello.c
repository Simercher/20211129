#include <stdio.h>
#include <string.h>
//#include <Windows.h>

//char outbuf[1000];
int main(void)
{
    char str[4];
    scanf("%s", str);
    for(int i = 0; i < 4; i++)
    {
        for(int j = 0; j < 4; j++)
        {
            if(i != j)
            {
                
            }
        }
    }

    return 0;
}